Checkpoint current work:

1. Run `git status` and show me what's changed
2. Suggest a clear commit message based on the changes
3. Wait for my approval before committing
4. After I approve, run `git add .` and `git commit -m "[message]"`
5. Ask if I want to push to remote
